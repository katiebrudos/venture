import { addACharacter } from '../../../helpers/add-a-character';
import { module, test } from 'qunit';

module('Unit | Helper | add a character');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = addACharacter(42);
  assert.ok(result);
});
