<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <span class="navbar-brand">
        Venture
      </span>
    </div>

    <ul class="nav navbar-nav">
		<li> {{#link-to 'authenticated.secrets'}} <span>Login</span> {{/link-to}} </li>
		{{#if session.isAuthenticated}}
		 <li> <a href="!#" {{action 'invalidateSession'}} >Logout </a></li>
		 <li> {{#link-to 'authenticated.map'}} <span>Map</span> {{/link-to}} </li>
       <li> {{#link-to 'authenticated.character'}} <span>Character</span> {{/link-to}} </li>
		{{/if}}
    </ul>

  </div>
</nav>

{{outlet}}
<footer>Copyright</footer>